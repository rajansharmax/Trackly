import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['images.unsplash.com', 'img.daisyui.com', 'randomuser.me', 'api.uifaces.co'],
  },
}


export default nextConfig;
